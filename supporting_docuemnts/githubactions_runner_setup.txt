aws cli 
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html
====================================================================================
docker installation

# Add Docker's official GPG key:
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

sudo apt update

sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

  sudo systemctl status docker

  sudo systemctl start docker

  sudo usermod -aG docker ubuntu


https://docs.docker.com/engine/install/ubuntu/
==================================================================================================

trivy installation
sudo apt-get install wget gnupg

wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | gpg --dearmor | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null

echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main" | sudo tee -a /etc/apt/sources.list.d/trivy.list

sudo apt-get update

sudo apt-get install trivy

================================================================================================
sonnar scanner installation

sudo apt install openjdk-17-jdk -y

need to isntall sudo apt install unzip

sonarsacaner is a binary we can find it here https://binaries.sonarsource.com/?prefix=Distribution/

wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-7.3.0.5189-linux-x64.zip


unzip sonar-scanner-cli-7.3.0.5189-linux-aarch64.zip

sudo mv sonar-scanner-cli-7.3.0.5189-linux-aarch64.zip sonar-scanner

sudo mv sonar-scanner /opt


# Edit your bash profile
sudo nano ~/.bashrc

# Add these lines at the end
export SONAR_SCANNER_HOME=/opt/sonar-scanner
export PATH=$PATH:$SONAR_SCANNER_HOME/bin

# Save and exit (Ctrl+X, then Y, then Enter)

# Reload the profile
source ~/.bashrc

=========================================================================================

OWASP Dependency Chek tool Installation

cd /opt

sudo wget https://github.com/jeremylong/DependencyCheck/releases/download/v12.1.0/dependency-check-12.1.0-release.zip

sudo unzip dependency-check-12.1.0-release.zip

# Edit your bash profile
nano ~/.bashrc

# Add these lines at the end
export DEPENDENCY_CHECK_HOME=/opt/dependency-check
export PATH=$PATH:$DEPENDENCY_CHECK_HOME/bin

# Save and exit (Ctrl+X, then Y, then Enter)

# Reload the profile
source ~/.bashrc


Create a symlink for easier access
sudo ln -s /opt/dependency-check/bin/dependency-check.sh /usr/local/bin/dependency-check


sudo dependency-check \
  --updateonly \
  --data /opt/dependency-check/data







docker installation


